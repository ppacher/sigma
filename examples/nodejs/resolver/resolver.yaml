# Id for the new function
id: resolver

# Execution environment (according to launcher configuration)
type: js

# Content of the function
content:
    # Load from file
    file: resolver.js
    #inline: exports.handler = function(data) { return data.toString('utf8'); }